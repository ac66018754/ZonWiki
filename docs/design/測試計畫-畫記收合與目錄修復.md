# 測試計畫：畫記跟隨收合 + 繪圖工具體驗 + 目錄修復（fix/note-annotations-and-toc）

> 2026-07-08。TDD：本計畫先行、Sub-Agent 審查後才寫測試與實作。
> 對應需求（使用者原話摘要）：
> - A. 畫記/螢光筆/形狀/文字框要跟著 `:::toggle`（`<details.md-toggle>`）收合一起隱藏（目前只有便利貼會）
> - B. 螢光筆可設定「畫直線」
> - C. 畫線/圖形/T(文字) 畫完先進「調整中」：滾輪調大小、左鍵點一下完成、完成後維持原工具模式
> - D. 按 T(文字) 要取消畫筆/形狀模式
> - E. 右鍵點一下取消畫筆/形狀/螢光筆/T(文字) 模式
> - F. 右下角「📖 目錄」鈕不得影響「全部展開/收合」狀態（使用者實測：全部展開後點目錄，整篇變回收合）
> - G. 章節目錄表預設不開啟，點「📖 目錄」才開
> - H. 純 toggle 結構筆記（prod 的 reamde）目錄表完全不出現 → summary 要進 TOC

## 已確認的根因（檔案:行號）

| 需求 | 根因 |
|---|---|
| A | `NoteOverlay.tsx:473-538` 的 DOM 錨點收合機制只涵蓋 sticky/slide，不含 drawing shapes 與 text 框 |
| F | `page.tsx:345-349` effect 以 `[allTogglesExpanded, previewHtml]` 為依賴，任何 previewHtml 識別變動都會把全部 details 強制同步成布林（含初載把 `:::toggle-open` 壓成收合）；實際觸發路徑待 Playwright 重現 |
| G | `page.tsx:272` `useState(true)` |
| H | `toc.ts:16` 只掃 `<h1-3>`；toggle 標題是 `<summary class="md-toggle-summary">` 純文字（`ToggleContainerExtension.cs:107`），整篇無 h1-3 時 `toc=[]` → TocPanel `return null` |

## 層級 1：純函式單元測試（零相依 Node spec，沿用 `frontend/scripts/noteNav.spec.mjs` 慣例）

先寫、先跑、確認 FAIL，再實作。

### 1a. `frontend/scripts/toc.spec.mjs` — `buildToc` 擴充（需求 H）
1. 回歸：只有 h1/h2/h3 的 HTML → 行為與現行完全一致（id 注入、唯一化、層級）。
2. 純 toggle 巢狀三層 → summary 依巢狀深度給 level 1/2/3，並注入唯一 id 到 `<summary>` 標籤。
3. h 標題與 toggle 混合 → TOC 依「文件順序」排列。
4. 巢狀深度 >3 的 summary 不收錄（與 h1-3 三層上限一致）。
5. id 碰撞（summary 與 heading 同名）→ `-2`、`-3` 遞增唯一化。
6. `<details class="md-toggle" open>`（toggle-open 產物）一樣被收錄。
7. 壞輸入韌性：未閉合的 details、summary 內含已跳脫字元（`&amp;` 等）→ 不噴錯、文字正確。

### 1b. `frontend/scripts/drawGeometry.spec.mjs` — `shapes.ts` 新純函式（需求 B/C）
1. `scaleShape(s, factor)`：free/line/rect/ellipse 以外接框中心縮放；factor=1 → 座標不變；`scaleShape(scaleShape(s,2),0.5)` ≈ 原形（誤差 <1e-6）。
2. `shapeAnchorPoint(s)`：free → 點列中位點；line → 兩端中點；rect/ellipse → 外接框中心。
3. 回歸：`normalizeShapes` 保留 `type:'line'` + `opacity`（螢光筆直線的持久化格式）。

## 層級 2：靜態檢查
- `npx tsc --noEmit`、`npm run lint`（worktree 內）。
- 本次無後端變更（`toc.ts` 註解說 server：實為 Next 前端內執行，不動 C#）。

## 層級 3：Playwright E2E（本地 worktree 實例：前端 3100、後端 5109、DB 5533 共用）

前置：qa 測試帳號登入；建立測試筆記「畫記收合測試」，內容含：三層巢狀 `:::toggle`、toggle 外的一般段落、一個 `## h2 標題`（混合結構，同時覆蓋 reamde 型與一般型）。

| # | 步驟 | 預期 |
|---|---|---|
| A-1 | 在內層 toggle 的文字上畫螢光筆、矩形、放文字框、貼便利貼 → 收合該 toggle | 四者全部隱藏；展開 → 全部恢復原位 |
| A-2 | 收合「外層」toggle（畫記在內層內容上） | 一樣隱藏（祖先鏈判定） |
| A-3 | 在 toggle 外段落上畫記 → 收合任意 toggle | 不受影響、不位移錯亂地被誤藏 |
| B-1 | 螢光筆 → 開「直線」開關 → 拖曳 | 成半透明直線；關閉開關 → 回自由螢光筆 |
| C-1 | 畫矩形放開 | 進調整中（有視覺提示）；滾輪 → 大小改變；左鍵點一下 → 完成；仍是矩形模式可直接再畫 |
| C-2 | 新增文字框輸入文字 → 滾輪 | 字級/框尺寸隨滾輪變化 |
| D-1 | 選畫筆 → 按 T | 畫筆模式解除（SVG 不再攔截指標）、文字框建立 |
| E-1 | 選螢光筆 → 在內文右鍵 | 模式取消、該次右鍵選單被抑制；模式已取消後右鍵 → 不再攔截 |
| F-1 | 點「⊞ 全部展開」→ 點「📖 目錄」開、再關 | 所有 details 仍為展開（用 DOM 驗證 `details[open]` 數量不變） |
| G-1 | 重新整理頁面 | TocPanel 不出現；點「📖 目錄」→ 出現；再點 → 消失 |
| H-1 | 開純 toggle 測試筆記的 TOC | 章節=各層 summary；點某章節 → 自動展開其祖先並捲動到位 |
| V-1 | 亮/暗主題、375px/1280px 各截圖；console 無錯誤 | 收整於 worktree `test-artifacts/`（含腳本） |

## 驗收門檻
- 層級 1 全 PASS（先 FAIL 後 GREEN 的過程留在 commit 訊息或輸出紀錄）。
- 層級 2 無錯誤。
- 層級 3 全項通過＋截圖存證；F-1 需先在未修復版重現原 bug（歸因），修復後複驗歸零。

## Sub-Agent 審查結論的採納（2026-07-08）

必補（已納入實作與測項）：
1. **擦除索引錯位**：渲染層過濾隱藏形狀時必須保留原始索引（隱藏者渲染 null，不得 filter 重排），否則整筆刪除會刪錯形狀。E2E 補：兩形狀、藏一個、整筆刪另一個 → 隱藏者存活。
2. **隱藏形狀不得被局部/框選橡皮擦誤擦**：以 `eraseVisibleOnly` 純函式隔離（spec 3a-3c 已涵蓋）。
3. **形狀錨點 key**：以「形狀 JSON 內容」為 key（非索引），擦除洗牌後舊 key 淘汰、新段以可見狀態重新錨定，不會認錯形狀。
4. **視窗外形狀**：錨點建立時機擴充為「畫完當下（items 變更）＋捲動/resize（節流）＋toggle」，漸進錨定；從未進過視窗的舊資料維持「無錨點＝永遠顯示」的保守行為（與便利貼既有語意一致，文件化）。
5. **E2E 客觀訊號**：調整中狀態加 `data-testid="overlay-adjusting"`；工具啟用狀態以工具鈕的 `tk-btn--primary` class 斷言；每種標註型別獨立斷言。
6. **H 與全域同步 effect 的交互**：F 修法改為「序號閘門」——只有明確點擊「全部展開/收合」才會批次寫 `details.open`，previewHtml/其他重繪不再觸發 → TOC 展開祖先後不會被打回。

計畫修正：
- 行號以 worktree（main, d1fc26b）為準重新核對（tocOpen＝page.tsx:116、全域同步 effect＝189-193），編輯前逐檔重讀。
- **F 的實作順序**：先完成其他修改並起本地服務，在「未修 page.tsx」上重現 F/G（git stash 切換），確認機制後才套用修復並複驗。若重現發現根因非 effect 依賴（如整段 remount），回頭補測項再修。
- 範圍保險絲：若 F root cause 牽動元件結構重構，本 PR 先出 A/B/C/D/E/G/H，F 獨立處理。

不採納（記錄理由）：
- `selectTool`/`inClosedToggle` 抽純函式下沉單元測試：本次為 hotfix，DOM 相依邏輯抽取＋stub 成本高於效益，E2E 已覆蓋（YAGNI；未來若再壞再抽）。
- 深度>3 的 summary 明確不注入 id（原樣輸出），已在 spec 案例 4 名稱中言明。

## 執行結果（2026-07-08）

- 層級 1：`toc.spec.mjs` 11 PASS、`drawGeometry.spec.mjs` 17 PASS（先 RED 後 GREEN）。
- 層級 2：`tsc --noEmit` 0 error；改動檔案 eslint 0 error。
- 層級 3（Playwright，worktree 實例 3100/5109）：
  - F-1 先於未修版**重現**（全部展開→點目錄→5/5 變 0/5），插樁歸因＝React 19 對 `dangerouslySetInnerHTML` 物件識別比較 → 任何重繪都重注入 innerHTML（位元組級比對：重寫內容與原內容完全相同）；修復後複驗 5/5 維持。
  - A-1/A-2/A-3：矩形/螢光直線/橢圓/文字框/便利貼於深層收合、外層收合皆正確隱藏並可逆恢復；toggle 外畫筆線不受影響。（過程插曲：誤觸多建一張便利貼導致一度誤判「便利貼不隱藏」，實查為兩張便利貼、機制本身正確——已刪除雜訊資料複驗。）
  - 擦除隔離：框選橡皮擦掃過隱藏形狀座標區 → 展開後 4 形狀完好；整筆刪除在「1 可見＋3 隱藏」下刪中可見者、隱藏者無恙、Ctrl+Z 復原。
  - B-1 螢光直線（斜拖仍兩點、opacity 0.4）；C-1 調整中（`overlay-adjusting` 出現、滾輪 rx 40→42.4、頁面零捲動、左鍵完成、工具保持）；C-2 文字框滾輪 24px→25px；D-1 按 T 解除畫筆模式；E-1 右鍵取消模式且僅在有模式時抑制選單。
  - G-1 目錄預設不開、點鈕開/關；H-1 純 toggle 筆記目錄 6 章節（層級 1/2/3 正確、含 toggle 外 h2、依文件順序）。
  - 亮（warmpaper）/暗（night）×1280、暗×375 截圖於 `test-artifacts/`；375 無橫向捲軸；console 0 error。

## 第二輪（2026-07-08）：畫記「跟著文字走」——使用者實測回饋修復

使用者於本地（prod 同版）重現：多層 toggle 下「只展開 §2」時畫記 → 全部展開後畫記視覺上跑到 §1 內容上。修法＝持久化文字錨點＋位移 rebase（詳見 DECISIONS 第二輪條目）。

執行結果：
- 單元：`drawGeometry.spec.mjs` 擴至 20 PASS（anchor 欄位保留/壞資料丟棄/scaleShape 保留）；toc 11 PASS；tsc/eslint 0 error。
- E2E 七步（Playwright，完整重演使用者操作）：部分展開畫記（螢光筆 +12/拖曳便利貼 +18）→ 收 §2 兩者隱藏 → 全部展開 §2 下移 844px 相對差不變 → 再收/再開正確 → 重載（預設全收）隱藏 → 重載後全部展開仍 +12/+18（跨 session）。
- 舊資料回退回歸：無錨點畫記維持第一輪行為（4→1→4）。
- 過程中的關鍵實測：新版 Chrome 收合 `<details>` 內容幾何查詢回傳非空矩形（hidden=until-found）→ 隱藏判定改 DOM 祖先鏈。
- 對抗式復審（1 HIGH/3 MEDIUM/2 LOW，0 CRITICAL）處置：HIGH（undo 跳回 rebase 前座標）→ setDrawingShapes 後排程重算，實測 undo 後其餘畫記 +12 不跳位；MEDIUM 互動守衛 → 補拖曳中 id 守衛；LOW 縮放不必要重錨 → 座標未變即跳過；MEDIUM TreeWalker 批次化與 reAnchor 消歧義 jsdom 測試 → 登記技術債（單人系統、實際筆記規模下成本可接受；重複文字消歧義繼承自既有 textAnchor 機制）。
